{{- if .Values.aggregator.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "walrus-node.fullname" . }}-aggregator
  labels:
    {{- include "walrus-node.labels" . | nindent 4 }}
    role: aggregator
spec:
  replicas:  {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "walrus-node.selectorLabels" . | nindent 6 }}
      role: aggregator
  template:
    metadata:
      annotations:
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "walrus-node.selectorLabels" . | nindent 8 }}
        role: aggregator
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "walrus-node.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: walrus-aggregator
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
          - /bin/sh
          - -c
          args:
          - |
            mkdir -p /etc/walrus/config
            cp -r -L /opt/walrus/config/* /etc/walrus/config
            exec /opt/walrus/bin/walrus --config /etc/walrus/config/client_config.yaml aggregator --bind-address 0.0.0.0:{{ .Values.aggregator.public_port }} --metrics-address 127.0.0.1:{{ .Values.aggregator.metrics_port }}
          ports:
            - name: rpc
              containerPort: {{ .Values.aggregator.public_port }}
              protocol: TCP
            - name: metrics
              containerPort: {{ .Values.aggregator.metrics_port }}
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /opt/walrus/config
              readOnly: true

      volumes:
        - name: config
          configMap:
            name: {{ include "walrus-node.fullname" . }}
{{- end }}
